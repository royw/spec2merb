#show_attributes
  = form_for(@var_singular, :action => rest_resource(@parent, @var_singular)) do
    #block
      = partial :form
    %p= submit "Update"
  #actions
    %ul
      %li= show_btn rest_resource(@parent, @var_singular)
